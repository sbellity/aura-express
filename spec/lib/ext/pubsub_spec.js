define(['aura/ext/pubsub'], function(pubsub) {
  'use strict';
  /*global describe:true, it:true, sinon: true, assert: true, should: true */
  
  describe("Pubsub API", function() {
    it("We should have specs for that too...");
  });

});